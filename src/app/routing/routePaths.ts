/** Canonical route path constants. Reference these instead of raw strings. */
export const ROUTE_PATHS = {
  /** Application landing route. */
  home: "/",
  /** Catch-all wildcard for unmatched routes. */
  notFound: "*",
} as const;
