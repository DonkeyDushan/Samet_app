/**
 * Per-character scales tracked in the game store (added later). Every character
 * has a "wealth" and a "regime" value that chosen answers can nudge up or down.
 */
export type ScaleId = "wealth" | "regime";
