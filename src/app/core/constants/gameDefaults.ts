import type { GamePhase } from "../types/gamePhase";

/** Phase the game starts in. */
export const INITIAL_PHASE: GamePhase = 1;

/** Starting value for every character scale before any answer applies. */
export const INITIAL_SCALE_VALUE = 0;
