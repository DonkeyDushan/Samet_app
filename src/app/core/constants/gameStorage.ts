/** localStorage key under which the persisted game state is stored. */
export const GAME_STORE_STORAGE_KEY = "samet-game-store";

/** Persisted-state schema version; bump when the shape changes to trigger a migration. */
export const GAME_STORE_VERSION = 1;
